import {
  generateWorkspaceAccountsMetadata,
  renderWorkspaceAccountsPage,
} from "../workspace-global-pages";

export { generateWorkspaceAccountsMetadata as generateMetadata };

export default renderWorkspaceAccountsPage;
