import {
  generateWorkspaceSettingsMetadata,
  renderWorkspaceSettingsPage,
} from "../workspace-global-pages";

export { generateWorkspaceSettingsMetadata as generateMetadata };

export default renderWorkspaceSettingsPage;
