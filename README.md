# Web Frontend

React + Vite + Tailwind CSS frontend for the Supabase-backed project.

## Commands

```bash
npm install
npm run dev
npm run build
```

## Environment

Create `web/.env` from `web/.env.example` and provide:

- `VITE_SUPABASE_URL`
- `VITE_SUPABASE_ANON_KEY`
